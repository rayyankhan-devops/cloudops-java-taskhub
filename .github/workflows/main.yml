name: Master Ci-java-cloudops workflow

on:
    push:
        branches:
            - main
    pull_request:
        branches:
            - main

jobs:
    gitleak-scan:
      uses: ./.github/workflows/gitleak-scan.yml
    
    linting:
      needs: gitleak-scan
      uses: ./.github/workflows/lint.yml